

struct Command{
    int x,y;
    int demandAmout;
    vector<int> productList;
};

int score = 0;
vector<Command> commands_copied

// Boucle : for(int t=0 ; t < T; t++)
for (auto it : commands_copied)
    if (it->demandAmount == 0) {
	score += round((T-t) * 100 / T);
	commands_copied.erase(it);
    }

	
  
	
