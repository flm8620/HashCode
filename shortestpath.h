#ifndef SHORTESTPATH_H
#define SHORTESTPATH_H

#include <vector>
std::vector<int> bestVisitOrder(std::vector<int>& distanceMatix,
                                std::vector<int> ware_cmd_distance);

#endif // SHORTESTPATH_H
